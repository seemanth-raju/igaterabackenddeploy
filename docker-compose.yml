services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - fingerprint_storage:/app/storage/fingerprints

  extractor:
    build: ./streamlit_extractor
    restart: unless-stopped
    ports:
      - "${EXTRACTOR_PORT:-8501}:8501"

volumes:
  fingerprint_storage:
